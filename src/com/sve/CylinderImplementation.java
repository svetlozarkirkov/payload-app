package com.sve;

import java.math.BigDecimal;

public class CylinderImplementation extends Cylinder {

    public CylinderImplementation(String name, BigDecimal radius, BigDecimal width, BigDecimal price) {
        super(name, radius, width, price);
    }
}
