package com.sve;

import java.math.BigDecimal;

public class BoxImplementation extends Box {

    public BoxImplementation(String name, BigDecimal width, BigDecimal depth, BigDecimal height, BigDecimal price) {
        super(name, width, depth, height, price);
    }
}
