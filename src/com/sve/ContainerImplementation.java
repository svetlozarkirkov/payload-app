package com.sve;

import java.math.BigDecimal;

public class ContainerImplementation extends Container {

    public ContainerImplementation(BigDecimal width, BigDecimal depth, BigDecimal height) {
        super(width, depth, height);
    }
}